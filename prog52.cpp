#include<iostream>
using namespace std;
//p52 is to print the first and last numbers from 13 to 17 (p35) using FOR loop
//spring break hw due April 14, 2018
int main(){
    int w;
    for(w=13;w<=17;w++){
        if (w==13)
            cout<<"The first is "<<w<<endl;
        if (w==17)
            cout<<"The last is "<<w<<endl;
      }
    return 0;
}
